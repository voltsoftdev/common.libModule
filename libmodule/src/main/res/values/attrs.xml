<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- CircleImageView -->

    <declare-styleable name="StrokeTextView">
        <attr name="strokeColor" format="color" />
        <attr name="textFillColor" format="color" />
    </declare-styleable>

    <declare-styleable name="CircleImageView">
        <attr name="civ_border_width" format="dimension" />
        <attr name="civ_border_color" format="color" />
        <attr name="civ_border_overlay" format="boolean" />
        <attr name="civ_fill_color" format="color" />
    </declare-styleable>

    <declare-styleable name="PagerSlidingTabStrip">
        <attr name="pstsIndicatorColor" format="color" />
        <attr name="pstsUnderlineColor" format="color" />
        <attr name="pstsDividerColor" format="color" />
        <attr name="pstsIndicatorHeight" format="dimension" />
        <attr name="pstsUnderlineHeight" format="dimension" />
        <attr name="pstsDividerPadding" format="dimension" />
        <attr name="pstsTabPaddingLeftRight" format="dimension" />
        <attr name="pstsScrollOffset" format="dimension" />
        <attr name="pstsTabBackground" format="reference" />
        <attr name="pstsShouldExpand" format="boolean" />
        <attr name="pstsTextAllCaps" format="boolean" />
    </declare-styleable>

    <!-- Reference to a style for the menu drawer. -->
    <attr name="menuDrawerStyle" format="reference" />

    <!-- Styleables used for styling the menu drawer. -->
    <declare-styleable name="MenuDrawer">

        <!-- Drawable to use for the background of the content. -->
        <attr name="mdContentBackground" format="reference" />

        <!-- Drawable to use for the background of the menu. -->
        <attr name="mdMenuBackground" format="reference" />

        <!-- The size of the menu. -->
        <attr name="mdMenuSize" format="dimension" />

        <!-- Drawable used as indicator for the active view. -->
        <attr name="mdActiveIndicator" format="reference" />

        <!-- Defines whether the content will have a dropshadow onto the menu. Default is true. -->
        <attr name="mdDropShadowEnabled" format="boolean" />

        <!-- The size of the drop shadow. Default is 6dp -->
        <attr name="mdDropShadowSize" format="dimension" />

        <!-- The color of the drop shadow. Default is #FF000000. -->
        <attr name="mdDropShadowColor" format="color" />

        <!-- Drawable used for the drop shadow. -->
        <attr name="mdDropShadow" format="reference" />

        <!-- The touch bezel size. -->
        <attr name="mdTouchBezelSize" format="dimension" />

        <!-- Whether the indicator should be animated between active views. -->
        <attr name="mdAllowIndicatorAnimation" format="boolean" />

        <!-- The maximum animation duration -->
        <attr name="mdMaxAnimationDuration" format="integer" />

        <!-- Drawable that replaces the up indicator -->
        <attr name="mdSlideDrawable" format="reference" />

        <!-- String to use as the up indicators content description when the drawer is open -->
        <attr name="mdDrawerOpenUpContentDescription" format="string" />

        <!-- String to use as the up indicators content description when the drawer is closed -->
        <attr name="mdDrawerClosedUpContentDescription" format="string" />

        <!-- Whether an overlay should be drawn as the drawer is opened and closed -->
        <attr name="mdDrawOverlay" format="boolean" />

        <!-- The position of the drawer -->
        <attr name="mdPosition" format="enum">
            <enum name="left" value="0" />
            <enum name="top" value="1" />
            <enum name="right" value="2" />
            <enum name="bottom" value="3" />
            <enum name="start" value="4" />
            <enum name="end" value="5" />
        </attr>
    </declare-styleable>

    <!-- Stroke TextView -->
    <declare-styleable name="CustomTextView">
        <attr format="boolean" name="textStroke"/>
        <attr format="float" name="textStrokeWidth"/>
        <attr format="color" name="textStrokeColor"/>
    </declare-styleable>

    <!-- Card Stack -->
    <declare-styleable name="SwipeStack">
        <attr name="allowed_swipe_directions" format="integer">
            <enum name="only_left" value="1"/>
            <enum name="only_right" value="2"/>
            <enum name="both" value="0"/>
        </attr>
        <attr name="animation_duration" format="integer"/>
        <attr name="stack_size" format="integer"/>
        <attr name="stack_spacing" format="dimension"/>
        <attr name="stack_rotation" format="integer"/>
        <attr name="swipe_rotation" format="float"/>
        <attr name="swipe_opacity" format="float"/>
        <attr name="scale_factor" format="float"/>
        <attr name="disable_hw_acceleration" format="boolean"/>
    </declare-styleable>

    <declare-styleable name="InsertForm">

        <attr name="formTitle" format="string" />
        <attr name="formTitleColor" format="color" />
        <attr name="formDescription" format="string" />
        <attr name="formTitleSize" format="dimension" />
        <attr name="formHint" format="string" />
        <attr name="formHintColor" format="color" />
        <attr name="formInsertTextSize" format="dimension" />

        <attr name="formInsertPaddingTop" format="dimension" />
        <attr name="formInsertPaddingBottom" format="dimension" />
        <attr name="formInsertPaddingLeft" format="dimension" />

        <attr name="formInsertTextType" format="enum">
            <enum name="email" value="0"/>
            <enum name="password" value="1"/>
            <enum name="shortMessage" value="2"/>
            <enum name="longMessage" value="3"/>
            <enum name="number" value="4"/>
        </attr>

        <attr name="formImeOptionType" format="enum">

            <enum name="actionNext" value="0"/>
            <enum name="actionDone" value="1"/>

        </attr>

        <attr name="formThemeColor" format="color"/>
        <attr name="formShapeColor" format="color"/>
        <attr name="formOrientation" format="enum">

            <enum name="horizontal" value="1"/>
            <enum name="vertical" value="2"/>

        </attr>

        <attr name="formInsertFormDrawable" format="reference|integer"/>
    </declare-styleable>


    <declare-styleable name="sampleAttribute">

        <attr name="sampleAttribute1" format="string" />
        <attr name="sampleAttribute2" format="integer" />
        <attr name="sampleAttribute3" format="float" />
        <attr name="sampleAttribute4" format="fraction" />
        <attr name="sampleAttribute5" format="color" />
        <attr name="sampleAttribute6" format="boolean" />
        <attr name="sampleAttribute7" format="dimension" />
        <attr name="sampleAttribute8" format="flags" />
        <attr name="sampleAttribute9" format="reference" />

        <attr name="sampleAttribute10" format="enum">
            <enum name="email" value="0"/>
            <enum name="password" value="1"/>
            <enum name="shortMessage" value="2"/>
            <enum name="longMessage" value="3"/>
        </attr>
    </declare-styleable>

    <declare-styleable name="ScrollHeaderLayout">

        <attr name="headerLayoutResource" format="reference"/>

    </declare-styleable>

    <declare-styleable name="MaterialDesignButton">

        <attr name="connerRoundRadius" format="dimension"/>
        <attr name="materialBackgroundColor" format="color"/>
        <attr name="materialStrokeColor" format="color"/>

    </declare-styleable>

    <declare-styleable name="SimpleRecyclerView">

        <attr name="listType" format="enum">

            <enum name="verticalList" value="1"/>
            <enum name="horizontalList" value="2"/>
            <enum name="gridList" value="3"/>

        </attr>

        <attr name="gridColCount" format="integer"/>

    </declare-styleable>

    <declare-styleable name="VideoPlayerDecorator">
        <attr name="playerType">
            <enum name="exoPlayer" value="0">exoPlayer</enum>
            <enum name="ffmpegPlayer" value="1">ffmpegPlayer</enum>
            <enum name="defaultPlayer" value="2">defaultPlayer</enum>
        </attr>
    </declare-styleable>

</resources>